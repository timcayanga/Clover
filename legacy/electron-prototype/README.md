# Archived Electron Prototype

This directory preserves Clover's original desktop proof of concept for historical reference.

It is not part of the production web application, is not deployed to Vercel, and should not receive new product work. Current development belongs in the repository's `web/` workspace.

If the prototype must be inspected:

```bash
npm install
npm run start
```

