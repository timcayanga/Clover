{
  "name": "clover",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix web run start",
    "typecheck": "npm --prefix web run typecheck",
    "qa:critical": "npm --prefix web run qa:critical-page-loading",
    "qa:data-engine": "npm --prefix web run qa:data-engine-safety && npm --prefix web run qa:data-engine-validation",
    "qa:browser": "npm --prefix web run qa:browser-compatibility",
    "qa:release": "npm --prefix web run qa:release",
    "qa:prepush": "npm --prefix web run qa:dependency-boundaries && npm run typecheck && npm run qa:release && npm run build",
    "qa:release:live": "npm --prefix web run qa:release:live"
  }
}
