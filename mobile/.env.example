# Only public values belong here. NEVER add database, Clerk secret, or billing keys.
EXPO_PUBLIC_API_URL=https://staging.clover.ph
# Set a publishable key from the SAME Clerk instance as the API.
# Leave unset for the fictional sample-data preview.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
